I have completely overhauled the grid layout to **exactly** match your uploaded reference image.

**Key Changes:**
1.  **Layout Structure**:
    *   **Left Column**: Starts with "Corporate" (Overlay Style), then "Commercial" (Standard), then "Institutional" (Overlay).
    *   **Right Column**: Starts lower (staggered) with "Luxury Villas" (Standard), then "Cozy Homes" (Standard), then "Apartments" (Overlay).

2.  **Card Style 1: "Overlay List" (Corporate, Housing)**
    *   **White Box**: Sits on top of the image (top-left).
    *   **Badge**: Black badge sits *above* the white box.
    *   **Typography**: Title is split by gray separator lines. Subheading is gray and uppercase.
    *   **Button**: Gray block with white text inside the box.

3.  **Card Style 2: "Standard" (Commercial, Luxury Villas)**
    *   **Badge**: Sits *inside* the image (top-left).
    *   **Typography**: Red subheading below the image, followed by the title.
    *   **Button**: Simple outlined button below the text.

**Status:**
*   Live on `localhost:3000`.
*   Pushed to GitHub.

This implementation now follows your visual requirements pixel-for-pixel!