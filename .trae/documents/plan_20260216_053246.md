I will verify the codebase integrity by performing the following steps:

1. **Environment Setup**

   * Install dependencies for both `apps/client` and `apps/server` to ensure the environment is ready for verification.

2. **Static Analysis & Build Verification**

   * **Client**: Run `npm run lint` to check for code quality issues and `npm run build` to verify the Next.js application compiles successfully.

   * **Server**: Run `npm run build` (TypeScript compilation) to ensure the backend code has no type errors.

3. **Test Execution**

   * Run the available Playwright tests in `apps/client` (`npm run test` or `npx playwright test`) to verify that the frontend pages render correctly.

   * *Note: Some tests might fail if they depend on a running backend or database, but this will help identify what parts are isolated and working.*

4. **Report & Fix**

   * Analyze the results from the build and test steps.

   * Fix any discovered linting errors, build failures, or simple test issues.

   * Provide a summary of the system's health.

