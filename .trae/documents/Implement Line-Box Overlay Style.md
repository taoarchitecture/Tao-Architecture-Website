I have updated the design to match the new image you provided with the **Black Borders** and **Line Boxes**.

**Specific Changes:**
1.  **Black Top Border**: Added a thick black border (`border-t-[8px] border-black`) to the top of every card.
2.  **Badge Placement**: Moved the black category badge (e.g., "CORPORATE") to overlap this top border (`-top-[14px]`), exactly like the reference.
3.  **"Line Box" Overlay Style**:
    *   Removed the big single white box.
    *   **Split the Title**: The title text is now split into separate lines (e.g., "Creating collaborative" / "and contemporary"), each inside its own white box with a gray bottom border.
    *   **Transparent Button**: The "See Projects" button is now a transparent box with a white border and white text, floating over the image, just like in the Corporate card example.

**Status:**
*   Live on `localhost:3000`.
*   Pushed to GitHub.

This layout is now highly custom and matches the sophisticated look of your reference image!